package com.modus.edeliveryclient;

import java.io.FileNotFoundException;
import java.io.IOException;
import java.util.concurrent.ExecutionException;
import org.junit.Test;



import JavaPapyrusR6ServerApi.Constants;

public class DocumentServerClientTest {

	
	@Test
    public void aGetDocument() throws InterruptedException, ExecutionException, FileNotFoundException, IOException {
        /*System.out.println("testing the creation of participant");
        DocumentServerClient docClient = new DocumentServerClient();
        int docId = 55044;
        int page = 0;
        int userId = 1000000;
        int docTypeId = Constants.DEFAULT_DOCUMENT_DISPLAY_TYPE_NORMAL;
        String extension = "docx";
        String filename = docClient.getDocument2File(docId, page, userId, docTypeId, extension);
        System.out.println(filename);
        */
    }
}
